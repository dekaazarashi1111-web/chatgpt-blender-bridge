# 毎回使う開始プロンプト

以下の枠内を新しいセッションへ貼り、参考画像と依頼を添えてください。続きの場合は作品 ID を指定します。作品 ID が未定なら、ChatGPT が作成します。

```text
GitHub の dekaazarashi1111-web/chatgpt-blender-bridge を使って制作してください。
最初に CHATGPT_JOB_PROMPT.md の「実行者向けルール」と、そこから参照される説明を読んでください。
登録済みの制作ツールから必要なものを選び、指示・入力・進捗・成果物を GitHub に保存してください。
続きの場合は既存の実行状態と最新の保存済み成果物を確認してから再開してください。

作品 ID: （新規なら「新規」、続きなら既存 ID）
依頼: （作りたいもの・今回の変更。参考画像を添付）
```

## 実行者向けルール

### 最初に読む

1. `README.md`、`EXECUTION_CONTRACT.md`、`tools.json`、`creative-job.schema.json`、`projects/studio-smoke/jobs/blender-v001/job.json`、`projects/studio-smoke/jobs/texture-v001/job.json`。
2. 対象作品の `projects/<project_id>/` にある制作指示・決定事項・入力・既存ジョブ・レビュー。
3. `workspace-state` ブランチの対象作品の状態と、対応する Actions 実行・Release manifest。ブランチがない場合は未作成として扱う。
4. 入力を追加する場合は `docs/INPUTS.md`、再開する場合は `docs/RESUME.md`。

使えるツール・操作・設定値は実ファイルから確認する。会話中の過去の説明より、現在の実装と実行記録を優先する。Material Maker など未登録のツールを利用済みと主張しない。

### 指示と入力を残す

- 作品 ID を固定し、目的、参考画像の意味、納品形式、寸法、品質基準を作品の brief に保存する。未指定項目は合理的な既定値を記録する。
- 決定事項・変更理由・うまくいかなかった方法を残す。会話履歴だけに依存しない。
- 添付画像を自分が見られることと、Actions がそのファイルを読めることを区別する。実バイトを取得して入力として保存できた場合だけ「画像を渡した」と扱う。
- アップロード経路が使えない場合はその点を明示し、画像が必要な処理は開始しない。画像を見て造形指示へ変換する制作と、画像そのものを加工する制作を混同しない。

### ジョブを作る

- 新規制作は schema version 2 の `projects/<project_id>/jobs/<job_id>/job.json` を使う。既存 v1 worker の起動をユーザーに要求しない。
- script、入力、必要な補助ファイルを先に揃え、ジョブ定義を最後に追加する。実行対象の commit を固定し、その `source_commit` を実行記録から追跡する。
- job ID は改版ごとに新しくする。実行済み ID を別内容に書き換えない。実行中または完了済みの同じジョブを重複投入しない。
- `tools.json` の登録済み操作を順番に組み合わせる。制作中に外部ソフトや素材をダウンロードする前提の script を書かない。
- 既定の制作予算は合計 18,000 秒、最大 19,800 秒。長時間処理は工程とチェックポイントで分割し、保存の余裕を確保する。
- Actions の起動に利用可能な GitHub の操作を使い、実行 URL を記録する。接続が dispatch 操作に対応していない場合、できたと装わず、対応する起動経路を確認する。

### 実行・確認・修正

- 終了したら実行状態、ログ、manifest、出力のサイズと SHA-256、各検証結果を確認する。
- Blender の成果物は保存後の開き直し検証と必要な方向のプレビューを確認する。テクスチャは画像の実体を確認し、用途に応じて継ぎ目・色・マスク・マップの内容を評価する。
- 参考画像との比較が必要な場合は実際のプレビューを開いて比較する。画像を見られない状態でレビューを承認しない。
- レビューには対象 job ID、source commit、snapshot の識別情報、確認した画像、判定理由、残課題を残す。旧版への承認を新版へ流用しない。
- 不一致は新しいジョブで修正する。過去の成功だけを根拠に今回の結果を成功扱いしない。

### 中断・再開・報告

- チェックポイントは制作ファイルと一緒に GitHub への保存が完了してから、再開可能と報告する。ローカルに存在するだけでは不足。
- 別セッションでは、まず既存の Actions が動いていないか確認する。実行中なら追跡し、失敗・停止なら最新の保存済みスナップショットから新しいジョブを作る。
- 再開には状態記録にある `release_tag`、`project_id`、`job_id`、`archive_sha256`、`manifest_sha256` をそのまま使用する。SHA-256 不一致を無視しない。
- 現セッションの制約で待機を続けられない場合は、実行 URL、作品 ID、保存済み地点、次に確認するものを残す。ChatGPT が切れた後も自動で判断や修正を続けるとは約束しない。
- 最後に制作結果、実際に行った検証、レビュー状態、成果物へのリンクを簡潔に報告する。失敗時は原因・最新保存地点・次の手順を残す。
