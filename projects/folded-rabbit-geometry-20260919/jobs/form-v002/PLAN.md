# form-v002

form-v001の最終Releaseを固定hashで同一project resumeする。受入済みの親Release importは再実行しない。読み込むモデルhashは76f7e0926c7ebdcc45b34e0f608ebe5b7de9c77d77999b859b0948b85dd6e3ef。入力WebPとpacked imageはsnapshotから継承して新出力にも保存するため、新たなinputsの重複ステージはない。

実画像レビューform-v001.mdの不一致を対象に、鼻梁/マズル/眼窩/頬/歯列、耳の厚みと前方の折れ、腕・掌・指・脚端・爪先、蝶ネクタイを修正。胴体、腹パネル、尾、関節等の未変更オブジェクトはメッシュと変換のfingerprint一致を検査する。新しい13画像は変更が全身・顔・耳・手・足すべてに影響するため必要である。

model.pyはrefine.pyを実行前に1箇所だけ補正する。ワールド座標で生成した歯をobject originで回すのではなく歯の中心周りで回す事前修正である。テンプレート、entry、展開後の実行ソースをすべて保存し、geometry_source.pyとBlender Textにも実際の展開後ソースを残す。すでに投入したjobを後から差し替えるものではない。

レンダーと納品の仕組みはv001の検証済みソースhashを確認して利用。現在の新モデルを読んで実行し、実行プログラムも今回の出力へ保存する。過去画像を新画像として表示しない。

3工程5400/7200/900秒、総予算18000秒。全身生成途中、顔、四肢、完成形状でcheckpoint。その後再読込、実画像比較、必要なら新規jobで修正。まだv002の実行・保存・視覚受入は未確認。
