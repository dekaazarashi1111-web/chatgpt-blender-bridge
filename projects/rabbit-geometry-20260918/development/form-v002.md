# form-v002 preparation

v001の実画像レビューから修正。新規ジョブ投入前にソースを保存する。

- v001の最終snapshot内のmodel.blendを実復元し、既存データを編集する。元のgen scriptを全部再実行するのではなく、hash一致した元ソースからASTの関数定義のみをhelperとして利用。
- 顔は眼窩の別球を廃止し、連続した額・頬・顎のケースを作り、口腔と眼窩を実際に彫り込む。顎の前端を約50mm後退、側壁を厚くし、奥へ向かって上がる口腔底を作る。鼻/マズル/歯列を相対的に修正。
- 固定した開口形状の連続性を優先し、下顎はケースと連続メッシュへ統合。後の編集用Mandible vertex groupを残す。今回は可動リグを納品する約束はしていない。手も連続メッシュとし3本の指の選択用groupを残す。
- 耳は正面幅と側面厚みを独立した断面で合わせる。厚みのある丸い上端、上部のみ前へ曲がる中心線、浅く丸めた縁を持つ実くぼみ。insertを底面の前へ配置。
- 足を低い丸底の球から幅広で平らな底の爪先形状へ修正。膝/肩のみ局所調整。保持すべき尾・接続部・耳のばねには前後のデータfingerprintチェック。
- 頭部close-upは頭だけを描画し、camera frustum検査とrender後のalpha境界検査を加える。全身三面は300px/mへ正確に対応させた比較図と輪郭overlayを出す。定量mask指標は誤差を含む補助であって再現率ではない。
- v001のhost/review両artifactを実ダウンロードして、exact manifest SHAを再確認し、45/45ファイルのsizeとSHA-256をこのセッションの展開ファイルとも照合した。model/render/packageログに正常終了と再読込passを確認した。
- 3本のv002スクリプトはローカルpython py_compileを通した。Blenderでの実行と新しい見た目の受入はまだこれから。共通runtime/workflowには変更を加えていない。
