# 進捗・引継ぎ

## 現在
初期設定と参照コピーをmainへ保存する段階。新作品 `violet-rabbit-replica-20260918`。制作jobはまだ追加していない。Release checkpointはまだない。ローカルのみのデータを保存済みと呼ばない。

## 次の作業
1. 参考比率に沿う新規造形・画像由来材質・検証・比較画像作成のスクリプトを保存。
2. 入力とscriptが揃った後にv2 job.jsonを最後にcommitしてpush triggerを確認。source commitとrun URLをここに記録。
3. Actions、tool_report、manifest readback、実画像を照合。問題を版別レビューに記録し新jobで修正。

## 再開
開始時点に実行中Actionsなし、新作品state.jsonは404。以後は必ずmainの本作品指示とworkspace-state/workspaces/violet-rabbit-replica-20260918/state.json、jobs、Actions、Release manifestを照合する。既存の別作品を今回の続きとして扱わない。
